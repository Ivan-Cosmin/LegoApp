spring.application.name=LegoAssistant

# ===============================
# 1. DATABASE SETUP (PostgreSQL) - Mark 5
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/lego_db
spring.datasource.username=postgres
spring.datasource.password=1q2w3e
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# 2. AI SETUP (Ollama) - Mark 8
# ===============================
# Ensure you have run: 'ollama run mistral' (or llama3) in your terminal
spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.model=qwen2.5:0.5b

# ===============================
# 3. SECURITY (Basic Setup) - Mark 10
# ===============================
# We will upgrade this to a database login later, but this lets us log in now.
spring.security.user.name=admin
spring.security.user.password=admin

# Increase upload limit to 20MB (Adjust as needed)
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB